# Dependencies & build
node_modules/
dist/

# Generated data snapshots — published to the `data` branch by CI, never to main
data/

# Secrets — never commit any credential or token file
.env
.env.*
!.env.example
token-cache.json
token_response.json
*.pem
*.key

# OS / editor
.DS_Store
*.local

# Per-machine Claude settings (.claude/launch.json IS shared and tracked)
.claude/settings.local.json
